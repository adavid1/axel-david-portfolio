[build]
  # Also prints the CV PDFs into dist/pdf/ (scripts/generate-cv-pdfs.mjs).
  command = "pnpm build"
  publish = "dist"

[functions]
  # Bundles the TypeScript modules that functions import from src/.
  node_bundler = "esbuild"
