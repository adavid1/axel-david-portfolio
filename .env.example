# Copy to .env.local and fill in. Used by the CV page (src/constants.ts) and by
# `pnpm cv:pdf`, which writes your personal PDFs to private/cv/.
# Leaving it unset simply omits the phone line from the CV.
VITE_CV_PHONE=
